name: 🚀 Construir, Enviar Imagens Docker e Deploy no Kubernetes (Universal)

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    env:
      NAMESPACE: app-demo
      DOCKER_USER: guisilva1k2k
      BACKEND_TAG: deploy
      FRONTEND_TAG: dev
      FRONTEND_URL: http://3.128.96.48:30080
      INFRA_BASE: https://raw.githubusercontent.com/guisilva1k2k/loja-infra/main/k8s

    steps:
      # 1️⃣ Baixar código do repositório
      - name: 1️⃣ Baixar código
        uses: actions/checkout@v3

      # 2️⃣ Configurar Docker Buildx
      - name: 2️⃣ Configurar Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 3️⃣ Login no Docker Hub
      - name: 3️⃣ Login no Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKER_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # 4️⃣ Buildar e enviar imagem do Backend
      - name: 4️⃣ Buildar e enviar imagem do Backend
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          push: true
          tags: ${{ env.DOCKER_USER }}/backend:${{ env.BACKEND_TAG }}
          no-cache: true
          pull: true

      # 5️⃣ Buildar e enviar imagem do Frontend (Angular)
      - name: 5️⃣ Buildar e enviar imagem do Frontend (Angular)
        uses: docker/build-push-action@v6
        with:
          context: ./client
          push: true
          tags: ${{ env.DOCKER_USER }}/frontend:${{ env.FRONTEND_TAG }}
          no-cache: true
          pull: true

      # 6️⃣ Instalar kubectl
      - name: 6️⃣ Instalar kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'latest'

      # 7️⃣ Configurar credenciais de acesso ao Kubernetes
      - name: 7️⃣ Configurar kubeconfig
        run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBE_CONFIG_DATA }}" | base64 --decode > $HOME/.kube/config
          sed -i 's/127.0.0.1/3.128.96.48/g' $HOME/.kube/config
          kubectl get ns $NAMESPACE || kubectl create ns $NAMESPACE
          echo "✅ kubeconfig aplicado com sucesso!"
          kubectl cluster-info --insecure-skip-tls-verify || echo "⚠️ Aviso: verifique o acesso ao cluster"

      # 8️⃣ Baixar manifestos Kubernetes automaticamente
      - name: 8️⃣ Baixar manifestos K8s (infra externa)
        run: |
          set -e
          mkdir -p k8s
          for f in 00-namespace.yaml 01-secret-mysql.yaml 02-configmap-app.yaml 03-configmap-mysql-init.yaml 04-configmap-mysql-dump.yaml 10-mysql-service.yaml 11-mysql-statefulset.yaml 20-backend-service.yaml 21-backend-deployment.yaml 30-frontend-service.yaml 31-frontend-deployment.yaml 50-job-mysql-import.yaml kustomization.yaml; do
            echo "⬇  Baixando $f"
            curl -fsSL "$INFRA_BASE/$f" -o "k8s/$f"
          done
          test -s k8s/kustomization.yaml || { echo "❌ Falha ao baixar kustomization.yaml"; exit 1; }
          echo "✅ Manifestos baixados com sucesso"

      # 9️⃣ Aplicar manifestos do Kubernetes
      - name: 9️⃣ Aplicar manifestos Kubernetes
        run: |
          echo "📦 Aplicando manifestos no cluster..."
          kubectl apply -k k8s --validate=false --insecure-skip-tls-verify
          echo "✅ Manifestos aplicados com sucesso"

      # 🔟 Atualizar imagens dos deployments
      - name: 🔟 Atualizar imagens nos deployments
        run: |
          echo "🔁 Atualizando imagens no cluster..."
          kubectl set image deployment/backend app=${{ env.DOCKER_USER }}/backend:${{ env.BACKEND_TAG }} -n $NAMESPACE --insecure-skip-tls-verify
          kubectl set image deployment/frontend web=${{ env.DOCKER_USER }}/frontend:${{ env.FRONTEND_TAG }} -n $NAMESPACE --insecure-skip-tls-verify
          echo "✅ Imagens atualizadas com sucesso!"

      # 1️⃣1️⃣ Verificar status dos deployments
      - name: 1️⃣1️⃣ Aguardar rollout dos deployments
        run: |
          echo "🕓 Aguardando os pods ficarem prontos..."
          kubectl rollout status deployment/backend -n $NAMESPACE --timeout=180s || kubectl logs -l app.kubernetes.io/name=backend -n $NAMESPACE --tail=50
          kubectl rollout status deployment/frontend -n $NAMESPACE --timeout=180s || kubectl logs -l app.kubernetes.io/name=frontend -n $NAMESPACE --tail=50
          echo "✅ Rollouts concluídos!"

      # 1️⃣2️⃣ Rodar Job de importação do dump SQL
      - name: 1️⃣2️⃣ Executar job SQL de importação (opcional)
        run: |
          echo "📥 Rodando importação do SQL dump no MySQL..."
          kubectl delete job mysql-import -n $NAMESPACE --ignore-not-found --insecure-skip-tls-verify
          kubectl apply -f k8s/50-job-mysql-import.yaml -n $NAMESPACE --validate=false --insecure-skip-tls-verify
          echo "✅ Job de importação aplicada!"

      # 1️⃣3️⃣ Mostrar URL pública do frontend
      - name: 1️⃣3️⃣ Resumo final do deploy
        run: |
          echo "✅ Backend: ${{ env.DOCKER_USER }}/backend:${{ env.BACKEND_TAG }}"
          echo "✅ Frontend: ${{ env.DOCKER_USER }}/frontend:${{ env.FRONTEND_TAG }}"
          echo "🌐 Acesse o frontend em:"
          echo "👉 ${{ env.FRONTEND_URL }}"
          echo "🚀 Deploy concluído com sucesso!"
